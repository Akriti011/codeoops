# Demo Fixture

A tiny fixture repository used by test_repo_facts.py. Not a real project —
just enough surface (a manifest, an entry point, an env-var read, a
pydantic-style settings class) to assert exact extraction.
